//  Insert / delete / update in arrays

const arr = ['mango', 'orange', 'tigernut', '4', 'banana'];
// Insert 10 at index 2


for (let i = 0; i < navigator; i++){
    for(let j = 0; j < navigator; j++){
        compare(i, j);
    }
}
